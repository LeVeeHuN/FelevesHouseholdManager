<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FelevesHouseholdManager.MainPage"
             xmlns:local="clr-namespace:FelevesHouseholdManager"
             x:DataType="local:MainPageViewModel">
    <ContentPage.Resources>
        <ResourceDictionary>
            <local:CompletedToColorConverter x:Key="CompletedToColorConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>

    <!--<Border>-->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <VerticalStackLayout Grid.Row="0" Grid.Column="0" HorizontalOptions="Center">
                <Label Text="TODOs" FontAttributes="Bold" FontSize="25"/>
                <Button Text="Open TODOs" Clicked="Button_Clicked" HorizontalOptions="Fill" Margin="10"/>
                <ScrollView>
                    <CollectionView ItemsSource="{Binding Todos}"
                                    SelectionMode="None">
                        <CollectionView.ItemsLayout>
                            <LinearItemsLayout Orientation="Vertical"/>
                        </CollectionView.ItemsLayout>

                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="local:Todo">
                                <VerticalStackLayout Padding="8" BackgroundColor="{Binding IsCompleted, Converter={StaticResource CompletedToColorConverter}}">
                                    <Label Text="{Binding Title}" FontAttributes="Bold"/>
                                    <Label Text="{Binding Description}"/>
                                    <Label Text="{Binding CreatedAt}"/>
                                </VerticalStackLayout>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </ScrollView>
            </VerticalStackLayout>
            <VerticalStackLayout Grid.Row="0" Grid.Column="1" HorizontalOptions="Center">
                <Label Text="Wishes" FontAttributes="Bold" FontSize="25"/>
                <Button Text="Open Wishes" Clicked="Button_Clicked_1" HorizontalOptions="Fill" Margin="10"/>
                <ScrollView>
                    <CollectionView ItemsSource="{Binding Wishes}"
                    SelectionMode="None">
                        <CollectionView.ItemsLayout>
                            <LinearItemsLayout Orientation="Vertical"/>
                        </CollectionView.ItemsLayout>

                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="local:Wish">
                                <VerticalStackLayout Padding="8" BackgroundColor="{Binding IsPurchased, Converter={StaticResource CompletedToColorConverter}}">
                                    <Label Text="{Binding Title}" FontAttributes="Bold"/>
                                    <Label Text="{Binding Description}"/>
                                    <Label Text="{Binding Price, StringFormat='{0} Ft'}"/>
                                    <Label Text="{Binding CreatedAt}"/>
                                </VerticalStackLayout>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </ScrollView>
            </VerticalStackLayout>
            <VerticalStackLayout Grid.Row="1" Grid.Column="0" HorizontalOptions="Center">
                <Label Text="Incomes" FontAttributes="Bold" FontSize="25"/>
                <Button Text="Open finances" Clicked="Button_Clicked_2" HorizontalOptions="Fill" Margin="10"/>
                <ScrollView>
                    <CollectionView ItemsSource="{Binding Incomes}"
                    SelectionMode="None">
                        <CollectionView.ItemsLayout>
                            <LinearItemsLayout Orientation="Vertical"/>
                        </CollectionView.ItemsLayout>

                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="local:Income">
                                <VerticalStackLayout Padding="8">
                                    <Label Text="{Binding Title}" FontAttributes="Bold"/>
                                    <Label Text="{Binding Description}"/>
                                    <Label Text="{Binding Price, StringFormat='{0} Ft'}"/>
                                    <Label Text="{Binding CreatedAt}"/>
                                </VerticalStackLayout>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </ScrollView>
            </VerticalStackLayout>
            <VerticalStackLayout Grid.Row="1" Grid.Column="1" HorizontalOptions="Center">
                <Label Text="Expenses" FontAttributes="Bold" FontSize="25"/>
                <Button Text="Open finances" Clicked="Button_Clicked_2" HorizontalOptions="Fill" Margin="10"/>
                <ScrollView>
                    <CollectionView ItemsSource="{Binding Expenses}"
                    SelectionMode="None">
                        <CollectionView.ItemsLayout>
                            <LinearItemsLayout Orientation="Vertical"/>
                        </CollectionView.ItemsLayout>

                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="local:Expense">
                                <Grid ColumnDefinitions="3*,*"
                                      Padding="8">
                                    <VerticalStackLayout Grid.Column="0">
                                        <Label Text="{Binding Title}" FontAttributes="Bold"/>
                                        <Label Text="{Binding Description}" />
                                        <Label Text="{Binding Price, StringFormat='{0} Ft'}"/>
                                        <Label Text="{Binding CreatedAt}"/>
                                    </VerticalStackLayout>
                                    <Image Grid.Column="1"
                                           WidthRequest="60"
                                           HeightRequest="60"
                                           Aspect="AspectFill"
                                           Source="{Binding ImagePath}" />
                                </Grid>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </ScrollView>
            </VerticalStackLayout>
        </Grid>
    <!--</Border>-->

</ContentPage>
